    {# No dynamic Values #}
    enum OrderDirection {
        ASC
        DESC
    }

    input PaginationInput {
        offset: Int!
        limit: Int!
    }

    interface GraphElement {
        id: ID!
        label: String!
    }